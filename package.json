{
    "name": "the-k",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "deploy": "npm run build && wrangler deploy"
    },
    "devDependencies": {
        "vite": "^5.0.0",
        "wrangler": "^4.53.0"
    },
    "dependencies": {
        "gsap": "^3.13.0"
    }
}
